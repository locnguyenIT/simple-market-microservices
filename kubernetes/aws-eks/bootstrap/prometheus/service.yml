apiVersion: v1
kind: Service
metadata:
  labels:
    prometheus: applications
  name: prometheus-service
  namespace: monitoring
spec:
  ports:
    - protocol: TCP
      port: 9090
      targetPort: 9090
  selector:
    prometheus: applications